// Shared constants injected into agent prompts and pipeline logic.
// Change values here to affect all agents that reference them.

export const NUMBER_OF_NOUNS = 2; // max nouns buildNounVerbExtractorAgent will extract
